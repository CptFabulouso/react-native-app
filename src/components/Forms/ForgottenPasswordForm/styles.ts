import { StyleSheet } from 'react-native';

export default StyleSheet.create({
	wrapperStyle: {
		marginBottom: 30,
		justifyContent: 'center',
		width: '100%',
	},

	button: {
		marginTop: 10,
	},
});
